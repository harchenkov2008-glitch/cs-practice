# cs-practice
git clone https://github.com/harchenkov2008-glitch/cs-practice.git cs-practice

git add labo2/NOTES.md
git diff - - staged
git commit -m “Describe local Git startes”
dit branch -M main

git switch -c explain-branches

git merge —-no-ff explain-branches -m “Merge branch explain-branches”
git log —-oneline —-graph
git branch -d explain-branches
git push
